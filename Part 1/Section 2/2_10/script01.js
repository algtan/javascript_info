if ("0") {
    alert( 'Hello' );
  }
// "0" is a non-empty string, so it is true

if (0) {
    alert( 'Hello 2');
 }
// 0 is the equivalent of false in Boolean, so it is false, and nothing should show up